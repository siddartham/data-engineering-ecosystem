[build-system]
requires = [
    "setuptools",
    "wheel",
    "pypandoc",
]
build-backend = "setuptools.build_meta"

[tool.black]
extend-exclude = "/nike/enablon_client/model.py"
line-length = 79
target-version = [
    "py37",
    "py38",
    "py39",
    "py310",
    "py311",
]

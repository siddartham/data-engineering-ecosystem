[build-system]
requires = [
    "setuptools",
    "wheel",
    "pip",
    "cython",
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 79
target-version = [
    "py38",
    "py39",
    "py310",
    "py311",
    "py312",
]

[tool.isort]
profile = "black"
src_paths = [
    "airflow",
    "data_quality_framework",
    "tests",
    "scripts",
    "setup.py",
]
line_length = 79
wrap_length = 79
multi_line_output = 3

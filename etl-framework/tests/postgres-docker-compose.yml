version: "3.1"
services:
    postgres:
        network_mode: bridge
        container_name: postgres
        restart: always
        image: postgres
        ports:
            - "5432:5432"
        environment:
            POSTGRES_DB: postgres,my_database_dev
            POSTGRES_USER: ci
            POSTGRES_PASSWORD: test

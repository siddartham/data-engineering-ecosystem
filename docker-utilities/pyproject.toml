[build-system]
requires = [
    "setuptools",
    "wheel",
    "pip",
    "cython",
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 79
target-version = [
    "py38",
    "py39",
    "py310",
]
